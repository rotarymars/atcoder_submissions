#include <iostream>
using namespace std;
signed main()
{
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);
    long double a,b;
    cin>>a>>b;
    cout<<(1-b/a)*100<<endl;
    return 0;
}
